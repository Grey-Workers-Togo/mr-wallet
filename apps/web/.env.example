# Base URL of the API. Falls back to http://localhost:3000/api/v1 if unset.
NEXT_PUBLIC_API_URL="http://localhost:3000/api/v1"

# Public URL of this app, used for canonical links, sitemap, robots.txt.
# Falls back to https://mister-wallet.com if unset.
NEXT_PUBLIC_APP_URL="http://localhost:3001"
